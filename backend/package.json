{
  "name": "workshop-inventory-api",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node scripts/migrate.js",
    "setup-app-role": "node scripts/setup-app-role.js",
    "bootstrap-admin": "node scripts/bootstrap-admin.js",
    "test": "node --test",
    "test:api": "node --test tests/api.integration.test.js",
    "test:security": "node --test tests/security.test.js tests/migration.security.test.js",
    "test:unit": "node --test tests/core.test.js",
    "test:watch": "node --test --watch"
  },
  "dependencies": {
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
